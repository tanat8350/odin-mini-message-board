<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <h1>
    <%= title %>
  </h1>
  <p>Welcome to <%= title %>
  </p>
  <a href="/new" rel="noopener noreferrer">New message</a>
  <% messages.forEach((e,i)=> { %>
    <p>
      [<%= e.added %>]
        [<%= e.user %>]
          <%= e.text %>
    </p>
    <% }); %>
</body>

</html>